# Cài torch (CUDA) TRƯỚC theo hướng dẫn ở README (tuỳ driver GPU từng máy), rồi mới
# `pip install -r requirements.txt`.

# --- Edge VLA: robot thật (SO-101/SO-100 follower qua Feetech bus) + policy runtime ---
lerobot==0.6.1
opencv-python==4.11.0.86   # cần bản có GUI (không phải -headless) vì viewer dùng cv2.imshow
pyyaml==6.0.2               # đọc config.yaml (robot/camera/policy)

# --- Edge VLA: MCP server ---
mcp==1.29.0
fastapi==0.139.0
uvicorn==0.51.0

# --- Cloud Agent: LangGraph + LLM client ---
langgraph==1.2.11
langchain-openai==1.5.1
sse-starlette==3.4.8
python-dotenv==1.2.2
